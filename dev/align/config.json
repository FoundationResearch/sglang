{
  "_comment": "1-layer OLMo-LHSA config used for HSA logits-alignment training. Keep this file deterministic — any change invalidates the trained weights and requires retraining (see README.md). The SHA-256 of this file is recorded in manifest.json so compare.py refuses to run with stale weights.",

  "vocab_size": 256,
  "hidden_size": 512,
  "intermediate_size": 2048,
  "num_hidden_layers": 1,
  "num_attention_heads": 8,
  "num_key_value_heads": 2,
  "head_dim": 64,
  "rms_norm_eps": 1e-6,
  "hidden_act": "silu",
  "tie_word_embeddings": false,
  "rope_theta": 10000.0,

  "chunk_size": 64,
  "hsa_topk": 4,
  "hsa_mode": "sparse",
  "full_attn_interleave": 1,
  "hsa_heads": 4,
  "hsa_qk_ratio": 4,
  "use_sliding_window": true,
  "sliding_window": 64,
  "num_swa_layers": 0,
  "enable_softmax1": true,
  "enable_lmk_q_proj": false
}
